package api

import (
	"context"

	"github.com/gabaghul/geolocation-golang/models"
)

// GetCityInfoByName returns info from requested city
func GetCityInfoByName(ctx context.Context, cityName string) (city models.GeoAPICity, err error) {
	return
}
